*.lck
pcb/fp-info-cache
oniondao-badge-backups/*

# macOS
.DS_Store

# Temporary files
*.000
*.bak
*.bck
*.kicad_pcb-bak
*.kicad_sch-bak
*-backups
*-cache*
*-bak
*-bak*
*~
~*
_autosave-*
\#auto_saved_files\#
*.tmp
*-save.pro
*-save.kicad_pcb
fp-info-cache
~*.lck
\#auto_saved_files#

# Netlist files (exported from Eeschema)
*.net

# Autorouter files (exported from Pcbnew)
*.dsn
*.ses

# Exported BOM files
*.xml
*.csv
# ...but keep ESP-IDF partition tables (firmware build config, not a BOM export)
!partitions.csv

# Archived Backups
**/*-backups/*.zip

# Local project settings
*.kicad_prl

# ESP-IDF firmware build artifacts (generated; also ignored per-project)
**/build/
**/.cache/
sdkconfig
sdkconfig.old
**/managed_components/
dependencies.lock

# Editor / IDE settings — machine-specific (absolute toolchain paths, serial
# ports). Each contributor generates their own; see
# software/guides/esp-idf-vscode-setup.md
.vscode/

# VS Code "Local History" extension snapshots
.history/
**/.history/
